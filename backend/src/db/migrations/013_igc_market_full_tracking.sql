-- ─────────────────────────────────────────────
-- 013_igc_market_full_tracking.sql
--
-- Документирует изменения в расчёте рыночного индекса IGC.
-- Схема БД не меняется (все нужные колонки уже есть из 012).
-- Изменения только в коде бэкенда.
--
-- До этого обновления igc_daily_supply/demand учитывали ТОЛЬКО:
--   supply:  добычу GPU (epochRunner)
--   demand:  электричество + обслуживание (epochRunner)
--
-- После — учитывают ВСЕ потоки IGC:
--   supply:  добыча GPU + реферальные бонусы L1/L2 + buy_igc
--   demand:  электричество + ремонт GPU + апгрейды GPU (паста/вентилятор/охлаждение)
--            + апгрейды инфры IGC (farm_level_2, workbench_1)
--            + создание синдиката + покупка бонусов синдиката
--            + роспуск синдиката (сжигание казны)
--            + sell_igc (возврат IGC в незаминченный пул)
--
-- Исправлено extra minting: реферальные бонусы теперь суммируются
--   в totalIgcProduced и totalIgcMinted, чтобы pool 10B отражал реальность.
--
-- Сглаживание индекса:
--   EMA α: 0.1 → 0.04 (~4ч полупериод, ~50 эпох)
--   rawRatio clamp: [0.3, 4.0] до EMA (защита от одиночных выбросов)
--   min data guard: не обновляем ratio если dailySupply+dailyDemand < 1 IGC
-- ─────────────────────────────────────────────

-- Нет структурных изменений БД — миграция является документацией.
SELECT 1; -- no-op
